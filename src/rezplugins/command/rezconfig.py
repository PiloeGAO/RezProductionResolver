"""Default settings for the plugin."""

production_resolver = {
    "production_database": "~/packages/prod.sqlite3",
    "staging_database": None,
    "history_folder": None,
    "keep_history": "true",
}
